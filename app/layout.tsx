import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Creative Brief Builder | Generate Brand Guidelines from Any Website",
  description: "Instantly generate comprehensive brand guidelines from any website URL. Extract colors, fonts, messaging, and more with AI-powered analysis.",
  keywords: ["brand guidelines", "brand strategy", "website analysis", "AI", "design system"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
